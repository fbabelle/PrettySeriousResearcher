# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
dist/

# OS
.DS_Store
Thumbs.db

# Machine-local / private
memory.md
.claude/settings.local.json
data/

# Maintainer-local (kept out of the public repo; the scaffold still writes a
# fresh changelog into paper projects, where it IS committed)
docs/changelogs.md

# Generated run data. docs/tracking/{state,cost}.json are the committed TEMPLATES
# install_skills.py --with-scaffold copies; the ledgers below are regenerated by
# track.py and belong to whichever machine ran it. (In a *paper* project the
# effort ledger IS committed — that .gitignore is written by the scaffold.)
docs/tracking/.session
docs/tracking/effort.jsonl
docs/tracking/effort.md
docs/tracking/cost.md

# Drafts
drafts/_archive/
